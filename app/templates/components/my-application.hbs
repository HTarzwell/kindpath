<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}
    <h1>{{#link-to "application" class="navbar-brand"}}<span class="kind">kind</span><span class="path">Path</span>{{/link-to}}</h1>

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        <li>{{#link-to "sign-up"}}Sign Up{{/link-to}}</li>
        <li>{{#link-to "sign-in"}}Sign In{{/link-to}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
<div class="holding col-md-12">
{{#if isAuthenticated}}
  <h2>How is your <span class="energy-head">energy level</span> today?</h2>
  <div class="energy-level">
    <button class="normal">{{#link-to "items"}}Normal{{/link-to}}</button>
    <button class="low" {{action toggleProperty 'lowEnergy'}}>{{#link-to "items.low"}}Low{{/link-to}}</button>
  </div>
{{/if}}

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

  {{outlet}}

</div>
