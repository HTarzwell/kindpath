import Route from '@ember/routing/route'

export default Route.extend({
  /* model () {
    return this.get('store').findAll('item')
  },
  renderTemplate: function (item) {
    item.get('store').filterBy('energy_needed', 'low')
  } */
  sortByLow: function (item) {

  }
})
